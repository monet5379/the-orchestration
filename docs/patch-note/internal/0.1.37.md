# Internal · v0.1.37

**날짜:** 2026-08-30  
**범위:** 매치 시작 동전 — 자동 던지기·스킵 → **입력으로 던지기 시작**  
**제품 표시:** `index.html` / `main.js?v=` / console → `0.1.37`

프로그래머용. **플레이어 체감 있음** → 외부 [`../external/0.1.39.md`](../external/0.1.39.md)에 포함.

화려한 SFX/연출은 **미포함** (후속). 패널 버튼은 **0.1.39**.

---

## 규칙 변경 요약

| 종류 | 내용 |
|------|------|
| **change** | `beginCoinCeremony` — 타이머 즉시 시작하지 않음. 입력으로 `START_COIN_TOSS` |
| **remove** | 의식 중 클릭 스킵(`skipCoinCeremony`) |
| **add** | `coinAwaitingInput` · `START_COIN_TOSS` |
| **change** | UX: 대기 →「수정 선공 결정…」→「첫 수정: …」→ SELECT |
| **keep** | `initiative` 50/50 · 턴 교대 · `COIN_TOSS_MS` / `COIN_RESULT_MS` |

---

## 상태 필드

| 필드 | 타입 | 의미 |
|------|------|------|
| `coinPending` | boolean | 매치 시작 의식 중. SELECT 잠금 |
| `coinAwaitingInput` | boolean | true면 던지기 대기. `START_COIN_TOSS` 후 false |
| `coinRevealed` | boolean | 결과 문구 노출 |

초기화: `createMatchState` (`coinPending: true`, `coinAwaitingInput: true`).

---

## 액션 · 흐름

1. `createMatchState` → `initiative` 50/50 · `coinPending` · `coinAwaitingInput`
2. `beginCoinCeremony` — 자동 타이머 없음 (0.1.39: 패널 버튼)
3. 입력 → `START_COIN_TOSS` → ~0.6s `REVEAL_COIN` → ~1.4s `FINISH_COIN` → SELECT

---

## 주요 파일

| 파일 | 역할 |
|------|------|
| `js/game/state.js` | `coinAwaitingInput` |
| `js/game/phases.js` | `START_COIN_TOSS` · FINISH 정리 |
| `js/main.js` | `startCoinToss` · `beginCoinCeremony` |
| `js/ui/renderer.js` | 대기·결과 카피 |

---

## 회귀 포인트

- [ ] 매치 시작: 자동으로 안 던져짐
- [ ] 입력 → 결정… → 첫 수정 → SELECT 타이머
- [ ] 턴2+: 동전 의식 없음 · 선/후 교대 유지

---

## 외부 후보

반영됨 — [`../external/0.1.39.md`](../external/0.1.39.md)

---

*Internal patch note · The Orchestration HTML Prototype · v0.1.37*
